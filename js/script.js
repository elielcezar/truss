(function () {

  
  document.addEventListener('DOMContentLoaded', () => {

    console.log('form js desativado');

    /*const observer = new MutationObserver(() => {
      
      const btnSalvarQrcode = document.querySelector('#block-truss-contentform #edit-actions')
      const btnUpload = document.querySelector('#block-truss-contentform #edit-field-foto-wrapper')

      if (btnSalvarQrcode && !btnSalvarQrcode.classList.contains('processed')) {
        btnUpload.insertAdjacentElement('afterend', btnSalvarQrcode)
        btnSalvarQrcode.classList.add('processed');
      }
    });    
    
    observer.observe(document.body, { childList: true, subtree: true });*/

  });
  

})();

